# Implementation

No runtime code is committed until the Tier 2 design validates. Version one begins with shared JSON contracts and deterministic policy tests, then adds QVAC and ExecuTorch adapters in separate commits.

